@import 'extras/*'
@import 'routes/*'

html, body, #app, .app
    height 100%

body
    font-family 'Roboto', sans-serif
    font-size 14px
    font-smoothing subpixel-antialiased
    font-weight 400
    line-height 14px
